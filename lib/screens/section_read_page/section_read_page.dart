import 'package:flutter/material.dart';

class SectionReadPage extends StatelessWidget {
  const SectionReadPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),body: Card(child: Center(child: Text('SectionReadPage'),),),);
  }
}